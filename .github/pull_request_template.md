## Description
<!-- Brief description of changes -->

## Type
- [ ] Bug fix
- [ ] New feature
- [ ] Documentation
- [ ] Refactor

## Checklist
- [ ] Tests pass (`uv run --frozen pytest`)
- [ ] Linting passes (`uv run --frozen ruff check .`)
- [ ] Type checking passes (`uv run --frozen pyright`)
- [ ] Frontend tests pass (`npm run test:run` and `npm run test:browser` in frontend/)
- [ ] Frontend builds without changing committed assets (`npm run build` in frontend/)
- [ ] CHANGELOG updated (if user-facing)
